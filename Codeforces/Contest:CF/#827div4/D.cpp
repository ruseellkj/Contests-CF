// D. coprime 
// codeforces #827 div 4

#include <bits/stdc++.h>
using namespace std;

int main(){
    int t;
    cin >> t;
    int n;
    while(t--){
        cin >> n;
        int a[n];
        for(int i=1; i<=n; i++){
            cin >> a[i];
        }

        // finding the index of maximum element in the array
        // distance(a,max_element(a+n));

        


    }
}